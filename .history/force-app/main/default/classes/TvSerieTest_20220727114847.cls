@isTest
public with sharing class TvSerieTest {
    @isTest static void checkName() {

        
        Database.SaveResult result= Database.insert(, false); 
        System.assert(result.isSuccess());
    }
}
